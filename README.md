# Troell — Performance Dashboard

Access-gated static dashboard (staticrypt-encrypted). The page content is
encrypted at rest and the data is **aggregate counts only — no PII**.
Generated from GA4 + Search Console + GoHighLevel.

Source generator: private monorepo `clients/troell/scripts/build_dashboard.py`.
